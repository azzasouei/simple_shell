#include "hsh.h"

/**
 * main -a main function
 * Return:always 0 (success)
 * @argc : input value
 * @argv : input value
 * @env : input value
*/
int main(int argc, char **argv, char **env)
{
	if (argc == 1)
	{
		terminal(argv, env);
	}
return (0);
}
